using System.Collections.Generic;

namespace GGJ
{
    public enum CrankState
    {
        NotStarted,
        Cranking,
        Finished,
    }

    public enum EnemyState
    {
        Active,
        NotActive
    }

    public enum MaskState
    {
        Off,
        On
    }

    public enum Layers
    {
        Player,
    };
}